(define (euler-cont-frac k)
  (cont-frac (lambda (i) 1.0)
             (lambda (i) (if (= 2 (remainder i 3))
                             (* 2 (/ (+ i 1) 3))) 
                             1)
             k))
