Under applicative-order evaluation, this expression will never evaluate, as (p) will forever recursively call itself. Under normal-order expression, this expression evaluates to 0, because the conditional is evaluated as true, so only the true-branch is evaluated, and the false branch is ignored.
